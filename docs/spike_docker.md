# Docker Compose Spike

> Sprint 0 과제: ELK + FastAPI Docker Compose PoC 검증

## 검증 목표

- [ ] `docker compose up` 후 Elasticsearch `/` 응답 확인
- [ ] Kibana UI 접근 가능 (`http://localhost:5601`)
- [ ] FastAPI `/health` 응답 확인 (`http://localhost:8000/health`)
- [ ] Logstash → ES 인덱싱 동작 확인

## 결과

_(Spike 완료 후 기록)_

## 시도한 접근법

_(기록 예정)_

## 성공 기준

`docker compose ps` 전체 서비스 `running` 상태 유지 5분
